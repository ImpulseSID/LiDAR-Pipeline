# ─── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ─── Virtual environments (recreated on Colab / locally) ─────────────────
.venv/
venv/
env/

# ─── Data (lives on Google Drive, NOT in git) ───────────────────────────
# The raw KITTI tracking frames, adversarial .bin frames and detector
# checkpoints are large binaries. Keep them on Drive and symlink `data/`
# into the repo on Colab (see notebooks/colab_task3.ipynb).
data/
*.bin
*.pcd

# ─── Model checkpoints ──────────────────────────────────────────────────
*.pth
*.ckpt
checkpoints/

# ─── Task3 generated outputs (reproducible, regenerated by the pipeline) ─
outputs/
*.png
!docs/**/*.png

# ─── OS / editor cruft ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
DiCon-FAT/